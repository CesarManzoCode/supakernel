{
  "name": "@supakernel/policy",
  "version": "0.0.0",
  "private": true,
  "description": "PolicyIR: validation, permissive/restrictive combination, field masks, PG native RLS + SQLite predicate rewrite (contract §13, §30 L4)",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit"
  },
  "sideEffects": false,
  "dependencies": {
    "@supakernel/contracts": "workspace:*",
    "@supakernel/ports": "workspace:*"
  },
  "devDependencies": {
    "@supakernel/contracts": "workspace:*",
    "@supakernel/ports": "workspace:*",
    "@supakernel/ports-test": "workspace:*",
    "@supakernel/db-postgres": "workspace:*",
    "@supakernel/db-sqlite": "workspace:*",
    "@types/node": "24.13.3",
    "vitest": "5.0.0"
  }
}
