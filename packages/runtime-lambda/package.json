{
  "name": "@supakernel/runtime-lambda",
  "version": "0.0.0",
  "private": true,
  "description": "AWS Lambda (Node 24 container) profile: API Gateway v2 ⇄ fetch adapter, env + limits (contract §10, §30 L10)",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit"
  },
  "sideEffects": false,
  "dependencies": {
    "@supakernel/contracts": "workspace:*"
  },
  "devDependencies": {
    "@supakernel/contracts": "workspace:*",
    "@types/node": "24.13.3",
    "vitest": "5.0.0"
  }
}
