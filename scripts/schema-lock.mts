/**
 * Regenerate packages/schema/schema.lock.json (canonical SchemaIR + hash) from the source SQL.
 * Implemented in L3. Node-only build tooling.
 */
process.stderr.write('schema:lock is implemented in L3 (schema / migrations layer)\n')
process.exit(1)
